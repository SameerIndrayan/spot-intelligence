spring:
  application:
    name: spot-intelligence

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:file:./data/spotdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update